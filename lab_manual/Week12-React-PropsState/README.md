# Week 12 - Props, Custom Components & State

- `ProductCard.jsx` shows a reusable React component that receives data using props.
- `ContactForm.jsx` shows a form with controlled inputs using `useState`.

Use inside a React project and render inside `App.jsx`.
