# Week 11 - React & Components

- `HelloWorld.jsx`: simple React component that prints "Hello World".
- `jsx-examples.jsx`: shows JSX with variables and functions.

Use them inside a React app (for example Vite or CRA) by importing
the component in `App.jsx` and rendering it.
